# PREVIEW_VALIDATION

These two preview PNGs are layout validation renders for the REV_10 HOME states.
They are not runtime backgrounds.

- HOME_GOLDEN_DEFAULT_430x932.png
- HOME_GOLDEN_SELECTED_430x932.png

The PWA runtime uses live DOM text.
Browser screenshot automation could not be completed in the current environment because local navigation was blocked by the administrator.
Therefore actual hosted URL visual regression remains required before RELEASE PASS.
